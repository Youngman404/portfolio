import React from 'react';
import Header from '../components/public/Header';

const Homepage = () => {
    return (
        <div>
            <Header></Header>
            Homepage Works !
        </div>
    );
};

export default Homepage;